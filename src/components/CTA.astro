---
const { ctaLink = "#comprar" } = Astro.props;
---

<section class="cta" id="comprar">
    <div class="container">
        <div class="cta-content">
            <h2 class="cta-title">🔥 Da el siguiente paso en tu carrera</h2>
            <p class="cta-text">
                <strong>Domina el rizo.</strong><br />
                <strong>Domina tu crecimiento.</strong><br />
                Tu evolución comienza aquí.
            </p>
            <a href={ctaLink} class="btn-cta">Comprar Ahora</a>
            <div class="cta-features">
                <div class="feature">
                    <span class="feature-icon">✓</span>
                    <span>Acceso inmediato</span>
                </div>
                <div class="feature">
                    <span class="feature-icon">✓</span>
                    <span>Garantía de 7 días</span>
                </div>
                <div class="feature">
                    <span class="feature-icon">✓</span>
                    <span>3 bonos incluidos</span>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .cta {
        padding: 100px 20px;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        text-align: center;
    }

    .container {
        max-width: 800px;
        margin: 0 auto;
    }

    .cta-content {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 2rem;
    }

    .cta-title {
        font-size: 2.5rem;
        font-weight: 800;
        margin: 0;
        line-height: 1.2;
    }

    .cta-text {
        font-size: 1.5rem;
        line-height: 1.6;
        margin: 0;
    }

    .cta-text strong {
        font-weight: 700;
    }

    .btn-cta {
        display: inline-block;
        background: #ff6b6b;
        color: white;
        padding: 20px 60px;
        font-size: 1.5rem;
        font-weight: 700;
        text-decoration: none;
        border-radius: 50px;
        transition: all 0.3s ease;
        box-shadow: 0 10px 40px rgba(255, 107, 107, 0.4);
        margin-top: 1rem;
    }

    .btn-cta:hover {
        background: #ff5252;
        transform: translateY(-3px);
        box-shadow: 0 15px 50px rgba(255, 107, 107, 0.5);
    }

    .cta-features {
        display: flex;
        gap: 2rem;
        flex-wrap: wrap;
        justify-content: center;
        margin-top: 1rem;
    }

    .feature {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        font-size: 1rem;
        font-weight: 600;
    }

    .feature-icon {
        background: rgba(255, 255, 255, 0.2);
        width: 24px;
        height: 24px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 0.875rem;
    }

    @media (max-width: 768px) {
        .cta {
            padding: 80px 20px;
        }

        .cta-title {
            font-size: 2rem;
        }

        .cta-text {
            font-size: 1.25rem;
        }

        .btn-cta {
            padding: 18px 48px;
            font-size: 1.25rem;
        }

        .cta-features {
            flex-direction: column;
            gap: 1rem;
        }
    }
</style>
