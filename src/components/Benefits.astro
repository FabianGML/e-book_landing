---

---

<section class="benefits">
    <div class="container">
        <h2 class="section-title">✨ ¿Qué aprenderás?</h2>
        <div class="benefits-grid">
            <div class="benefit-card">
                <div class="benefit-icon">🎯</div>
                <h3>Leer el rizo</h3>
                <p>Entender su comportamiento real y trabajar con precisión.</p>
            </div>

            <div class="benefit-card">
                <div class="benefit-icon">🔍</div>
                <h3>Diagnosticar con precisión</h3>
                <p>
                    Identificar patrones, texturas y porosidad de forma
                    profesional.
                </p>
            </div>

            <div class="benefit-card">
                <div class="benefit-icon">✂️</div>
                <h3>Cortes personalizados</h3>
                <p>Elegir el corte correcto según densidad y encogimiento.</p>
            </div>

            <div class="benefit-card">
                <div class="benefit-icon">💫</div>
                <h3>Técnicas profesionales</h3>
                <p>Domina rake & shake, shingling, finger coils y más.</p>
            </div>

            <div class="benefit-card">
                <div class="benefit-icon">🔧</div>
                <h3>Reconstrucción de rizos</h3>
                <p>Repara rizos dañados y crea terapias personalizadas.</p>
            </div>

            <div class="benefit-card">
                <div class="benefit-icon">💰</div>
                <h3>Servicios premium</h3>
                <p>
                    Diseña servicios de alto valor y aumenta tu ticket promedio.
                </p>
            </div>
        </div>
    </div>
</section>

<style>
    .benefits {
        padding: 80px 20px;
        background: #f8f9fa;
    }

    .container {
        max-width: 1200px;
        margin: 0 auto;
    }

    .section-title {
        text-align: center;
        font-size: 2.5rem;
        font-weight: 800;
        color: #2d3748;
        margin-bottom: 3rem;
    }

    .benefits-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 2rem;
    }

    .benefit-card {
        background: white;
        padding: 2rem;
        border-radius: 16px;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
        transition: all 0.3s ease;
        text-align: center;
    }

    .benefit-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 8px 30px rgba(0, 0, 0, 0.12);
    }

    .benefit-icon {
        font-size: 3rem;
        margin-bottom: 1rem;
    }

    .benefit-card h3 {
        font-size: 1.5rem;
        font-weight: 700;
        color: #2d3748;
        margin-bottom: 0.75rem;
    }

    .benefit-card p {
        font-size: 1rem;
        line-height: 1.6;
        color: #4a5568;
        margin: 0;
    }

    @media (max-width: 768px) {
        .benefits {
            padding: 60px 20px;
        }

        .section-title {
            font-size: 2rem;
        }

        .benefits-grid {
            grid-template-columns: 1fr;
            gap: 1.5rem;
        }
    }
</style>
